name: Deploy

# Manual trigger only - no automatic builds
on:
  workflow_dispatch:  # Can trigger manually from GitHub Actions tab
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging

jobs:
  deploy:
    name: Deploy to ${{ github.event.inputs.environment || 'production' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      # 🚀 DEPLOYMENT STEP
      # Replace this with your actual deployment method
      # Examples below (uncomment the one you need):

      # Option 1: Vercel
      # - name: Deploy to Vercel
      #   run: npx vercel --prod --token=${{ secrets.VERCEL_TOKEN }}

      # Option 2: Netlify
      # - name: Deploy to Netlify
      #   run: npx netlify-cli deploy --prod --auth=${{ secrets.NETLIFY_TOKEN }}

      # Option 3: SSH to your server
      # - name: Deploy via SSH
      #   uses: appleboy/ssh-action@v1.0.0
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     script: |
      #       cd /var/www/myapp
      #       git pull
      #       npm ci
      #       npx prisma generate
      #       pm2 restart myapp

      # Placeholder - shows deployment completed
      - name: Deployment Complete
        run: |
          echo "🚀 Deployment to ${{ github.event.inputs.environment || 'production' }} successful!"
          echo "Skipping build step - building happens on deployment platform"